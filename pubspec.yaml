name: passputter
description: Easily authenticate using OAuth 2.0 client/password grants.
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dev_dependencies:
  import_sorter: ^4.5.0
  test: ^1.17.3
  very_good_analysis: ^2.0.3

import_sorter:
  emojis: true
